# policy-repo

## Purpose
Holds all modules, logic, and configuration files related to the Policy simulation project.

## Structure
- `modules/` – simulation modules
- `schemas/` – data schema definitions
- `configs/` – configuration files
- `docs/` – documentation assets

This section is tagged for future CI/CD pipeline inclusion. Integration will be configured after module uploads.
